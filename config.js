/* =========================================================
   データ取得元の設定
   ---------------------------------------------------------
   GitHubリポジトリを作ったら、dataUrl に sr.json の「raw」URLを設定する。
   これで「Actionが5分ごとに更新するsr.json」をフロントが直接読む。
   （サイト本体は再デプロイ不要・raw側が約5分キャッシュ）

   例（ユーザー名/リポジトリ名 を自分のものに置換）:
   dataUrl: "https://raw.githubusercontent.com/yourname/fx-sr-tool/main/sr.json"

   空文字のままなら、同じフォルダのローカル sr.json を読む（開発用）。
   ========================================================= */
window.SR_CONFIG = {
  dataUrl: ""
};
